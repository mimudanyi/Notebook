[TOC]



# 一、删除线

使用方法：
	\~\~被删除的文字~~

# 二、表情符号

使用方法：
	:+ 表情代码+:

#  三、自动链接

> 自动链接只识别以www或http://开头的URL地址。如果不想使用自动链接，也可以使用\``包裹URL地址如下。
> `www.baidu.com`

使用方法：
	在标准语法中，由<>包裹的URL地址被自动识别并解析为超链接，使用GFM扩展语法则可以不使用<>包裹。

# 四、表格

语法说明如下。
1、单元格使用|来分隔，为了阅读更清晰，建议最前和最后都使用|。
2、单元格和|之间的空格会被移除。
3、表头与其他行使用-来分隔。
4、表格对齐格式如下
		左对齐（默认）：:-
		右对齐：-:
		居中对齐：:-:
5、块级元素（代码区块、引用区块）不能插入表格中。

使用方法：
	\|表头一|表头二|表头三|
	\|---------|---------|---------|
	\|内容一|内容二|内容三|
	\|内容一|内容二|内容三|

# 五、任务列表（没有成功实现这个语法！！！）

> 1、x可以小写，也可以大写，有些编辑器可能不支持大写，所以为避免解析错误，推荐使用小写的x。
> 2、当方括号中的字符为空格时，复选框是未选中状态，为x时是选中状态。

使用方法：
	\-+ 空格+ [+空格]+ 空格+ 任务列表内容
	-+ 空格+ [+x] + 空格+ 任务列表内容

# 六、围栏代码块

使用方法一：
	\```
	代码块
	\```

使用方法二：
	\~~~
	代码块
	\~~~

# 七、锚点（书签）

> 语法说明：
> 1、锚点名建议使用字母和数字，当然中文也是被支持的，但不排除有些网站支持得不够好。
> 2、锚点名是区分英文大小写的。
> 3、在锚点名中不能含有空格，也不能含有特殊字符。

使用方法：
	\[锚点描述](#锚点名)























