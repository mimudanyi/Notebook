# SQL Server

## 一、SQL Server 2014的组件：

1.SQL Server数据库引擎

2.分析服务（Analysis Services）

3.报表服务（Reporting Services）

4.集成服务（Integration Services）

5.主数据服务（Master Data Services）

## 二、SQL Server 2014的管理工具：

1.SQL Server Management Studio（SSMS）

2.SQL Server 配置管理器

3.SQL Server Profiler

4.数据库引擎优化顾问

5.数据质量客户端

6.SQL Server Data Tools

7.连接组件

## 三、SQL Server 的安装：

建议把SQL Server 2014安装在NTFS文件系统的计算机上，虽然FAT32文件系统也支持安装SQL Server 2014，但出于安全考虑，一般不建议在这种文件系统上安装。

## 四、在SSMS中可以用两种方式操作SQL Server

1.基于鼠标的可视化操作

2.代码操作：指通过编写SQL代码来操作SQL Server

## 五、SQL Server中的数据库分类：

（一）、系统数据库：

1.主数据库（Master）：

保存了SQL server所有系统信息、所有数据库文件的，同时还记录了SQL server的初始化信息。

2.模板数据库（Model）：

作为新创建数据库的一种模板或原型。

3.数据库（Msdb）：

用于安排SQL server 的周期活动，包括任务调度、异常处理和报警管理等。

4.临时数据库（Tempdb）：

用做系统的临时存储空间，其主要作用是存储用户建立的临时表和临时存储过程。

（二）、用户数据库：

## 六、SQL Server的文件组成：

（一）、主数据文件：

扩展名：.MDF

作用：主数据文件用于存放数据，是所有数据库文件的起点。

允许的个数：有且仅有一个

（二）、辅助数据文件：

扩展名：.NDF

作用：辅助数据文件也用来存放数据，存储主数据文件未存储的所有其它数据和对象。

允许的个数：可有可无，若有可有多个

（三）、事务日志文件：

扩展名：.LDF

作用：事务日志文件用来存放事务日志，用来记录SQL Server中所有事务日志文件的事务和由这些事务引起的数据库的变化。

允许的个数：至少有一个

## 七、文件组：

文件组允许将多个数据文件归纳为同一组，并赋予此组一个名称，这就是文件组。但是需要注意的是事务日志文件不属于文件组。

（一）、文件组类型：

1.主文件组：

主文件组是SQL Server 自动创建的，名字叫PRIMARY，主文件组_上包括主数据文件和所有没有被包括在其他文件组中的文件。

2.用户自定义文件组：

用户自定义文件组是为了提高数据库的性能，用户自己创建的，它包括创建或修改数据库时使用FileGroup关键字指定的文件组。

 

 

 

 

 