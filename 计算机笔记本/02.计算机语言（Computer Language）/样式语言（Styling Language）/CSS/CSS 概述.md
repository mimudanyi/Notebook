# CSS 概述

Cascading Style Sheets（层叠样式表，CSS）

插入样式表的方法有三种:

- 外部样式表(External     style sheet)

- 内部样式表(Internal     style sheet)

- - id选择器
  - 标签选择器
  - 类名选择器

- 内联样式(Inline style)

注释

```css
/*
	注释
*/
```

